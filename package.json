{
  "name": "diatom_whiteboard_server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "pusher": "^2.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
